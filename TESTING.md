# 测试文档

本项目主要以浏览器手动验证为主，以下用例覆盖主题切换、首页推荐、搜索、播放与历史记录等核心流程。

## 1. 基础环境
1. 运行服务：`npm run dev` 或 `npm start`
2. 访问首页：`http://localhost:3000`（以实际端口为准）

## 2. 主题切换
1. 切换主题命令：
   - `npm run theme:cinema`
   - `npm run theme:ocean`
   - `npm run theme:forest`
   - `npm run theme:sunset`
2. 刷新任意页面，检查以下元素是否同步变色：
   - 背景渐变
   - 按钮与高亮边框
   - 面板背景与边框
3. 预期：所有页面风格一致，无残留旧主题颜色

## 3. 首页推荐区（豆瓣）
1. 首页加载后确认推荐区展示数量增加（默认为 24）
2. 推荐区默认居中，左右视觉对称
3. 鼠标滚轮上下滚动时，推荐区横向滑动
4. 右侧封面不被遮挡
5. 封面加载失败时显示站点 Logo 作为占位

## 4. 搜索功能
1. 输入关键字并回车
2. 预期：搜索结果出现，数量统计正确
3. 结果列表为网格布局，分页按钮可用
4. 点击分页按钮能切换结果页

## 5. 详情与播放
1. 点击任意搜索结果卡片
2. 预期：详情弹窗出现，标题与来源正确
3. 点击某集进入播放页
4. 播放页顶部返回按钮可返回上一页

## 6. 历史记录
1. 播放后回到首页，打开历史记录
2. 点击历史记录条目进入播放页
3. 预期：播放页不会出现首页的悬浮按钮

## 7. 回归检查（常见问题）
1. 右上角设置按钮不被遮挡
2. 搜索不会提示“搜索请求失败”
3. 推荐区封面加载稳定

## 8. 建议记录
如发现异常，记录以下信息：
1. 发生页面与操作路径
2. 浏览器控制台报错
3. 发生时间与是否可复现
